/* ============================================================
   config.js — frontend runtime configuration
   ------------------------------------------------------------
   Set API_BASE_URL to your deployed backend (Render) URL.

   • Local development:  leave as "" — the app auto-detects localhost
     and talks to http://localhost:5000.
   • Production (Vercel): set this to your Render backend URL, e.g.
        API_BASE_URL: "https://manzar-portfolio-backend.onrender.com"
     NO trailing slash.
   ============================================================ */
window.PORTFOLIO_CONFIG = {
  // 👉 EDIT THIS for production (your Render backend URL, no trailing slash):
  API_BASE_URL: "",
};
