# The backend now runs as a Vercel serverless function (api/index.js -> backend/app.js),
# so the backend SOURCE must be shipped. Only exclude things the function doesn't need:
backend/node_modules/
backend/.env
backend/test/
backend/uploads/
backend/render.yaml
*.zip
